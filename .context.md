# Project Context

## Session 2026-05-21-14:00

### Summary

Resolved MCP visibility issue - server now shows as Connected in Gemini CLI

### Current State

MCP server connected and working in Gemini CLI (v0.42.0). 4 tools registered: save_context, read_context, get_context_status, append_context_note. 1 resource: context://current.

Server path: /home/rafa/sistema/unifymemfile/dist/index.js
Node version: v25.1.0 (via nvm)

### Recent Changes

Added MCP resource support (context://current) for reading context file directly via @server://resource/path syntax. Refactored index.ts with setupTools() and setupResources() methods separated. Updated install.ts with interactive IDE installer. Added serializeContextFile and generateDateKey to markdown.ts. Build successful, MCP server Connected.

### Decisions

Use `gemini mcp add` CLI command to register MCP servers (not manual JSON config). The manual config in ~/.gemini/antigravity/mcp_config.json is NOT picked up automatically - must use the CLI.

### Open Tasks

Verify /mcp command shows all registered MCP servers in interactive mode

### Notes

unifymemfile is a context persistence tool for IDEs. Maintains .context.md in project root to track sessions, decisions, changes, and tasks across coding sessions. Works as MCP server with Gemini CLI, VS Code, and other MCP-enabled tools.

KEY CONFIGURATION:
- MCP servers registered via: `gemini mcp add <name> node <path>`
- Server registered for current project: unifymemfile -> node /home/rafa/sistema/unifymemfile/dist/index.js
- MCP config stored in project settings (not ~/.gemini/antigravity/mcp_config.json)

INSTALLATION:
- npm install -g @rafa/unifymemfile (when published)
- Or: npx @rafa/unifymemfile
- Local dev: npm run build && gemini mcp add unifymemfile node /home/rafa/sistema/unifymemfile/dist/index.js

IMPORTANT FIXES APPLIED:
1. MCP visibility issue: Use `gemini mcp add` not manual JSON config
2. Project registered in settings.json via project name "unifymemfile"
3. Build required after changes: npm run build

PROJECT STRUCTURE:
- src/index.ts: Main MCP server with setupTools() and setupResources()
- src/markdown.ts: Context file read/write operations
- src/context.ts: saveContext function
- src/cli.ts: CLI commands for install/uninstall
- src/install.ts: Installation logic
- dist/: Compiled JavaScript (generated by npm run build)

KEY FUNCTIONS:
- serializeContextFile(): Formats ContextData to markdown string
- generateDateKey(): Returns "YYYY-MM-DD-HH" format timestamp
- readContextFile(): Reads and parses .context.md
- writeContextFile(): Writes context data to .context.md
- resolveProjectRoot(): Finds project root via .git or package.json

